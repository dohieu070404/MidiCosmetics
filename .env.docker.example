# Copying this file verbatim is intentionally not enough: blank secrets make
# Docker fail closed. Run scripts/setup-local.ps1 (Windows) or
# scripts/setup-local.sh (macOS/Linux) to generate .env.docker.local securely.
POSTGRES_PASSWORD=
DATABASE_URL=
DIRECT_URL=
JWT_ACCESS_SECRET=
JWT_REFRESH_SECRET=
